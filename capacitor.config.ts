import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Labwork3&4_Gmail_clone',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
